<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Khatabook</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <section>
        <!-- sidebar -->
        <div class="w-[280px] h-screen bg-neutral border-r fixed">
            <div class="h-14 border-b flex items-center">
                <img class="h-8 px-2" src="https://www.gstatic.com/images/branding/product/2x/docs_2020q4_48dp.png" alt="">
                <div class="">Khatabook</div>
            </div>
            <div class="px-2 mt-5">
                <a href="/create" class="bg-neutral-200 py-2 px-4 w-full inline-block rounded-md">Create New Doc</a>
            </div>
            <!-- list files -->
            <div class="mt-3">
                <%for(let i=0; i<files.length; i++){%>
                    <a href="/edit/?fileName=<%=files[i]%>">
                        <div class="border mx-2 mb-2 bg-blue-10 p-2 rounded-md"><%=files[i]%></div>
                    </a>
                <%}%>
            </div>
        </div>
        <!-- main container -->
         <div style="width:calc(100% - 280px)" class="ml-[280px] h-screen">
            <div class="h-14 border-b flex items-center">
                <%if(typeof fileName != 'undefined' && fileName){%>
                    <div class="pl-3"><%=fileName%></div>
                    <%}%>
            </div>
            <div class="mt-5 mx-5">
                <%- body %>
            </div>
         </div>
    </section>
</body>
</html>